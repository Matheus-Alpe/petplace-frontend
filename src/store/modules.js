import { store as auth } from '@/modules/auth'
import { store as user } from '@/modules/user'
import { store as pet } from '@/modules/pet'
import { store as vetHistory } from '@/modules/vethistory'

export default {
    auth,
    user,
    pet,
    vetHistory
}